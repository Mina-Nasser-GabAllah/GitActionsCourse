name: Commands workflow
on:
  workflow_dispatch:

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: annotaions example
        run: |
          deprecated=true
          if [[ depreacted ]]; then
            echo ::warning:: Deprecated fuction found
          fi 

          if [[ depreacted ]]; then
            echo ::error:: Error found
          fi
            
          if [[ depreacted ]]; then
            echo ::notice:: Notice found
          fi
      
      - name: Set and Mask API_KEY
        run: |
          echo "API_KEY=1234" >> $GITHUB_ENV
          echo "::add-mask::1234"
      
      - name: Use API_KEY
        run: |
          echo "Using API_KEY: $API_KEY"