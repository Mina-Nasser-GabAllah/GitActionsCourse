name: First workflow
on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps: 
      - name: Print hello message
        run: echo "Welcome to our first Github Actions"
      
     
        
